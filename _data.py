LEGACY_HTML = True  # 既存HTMLを保持（再アセンブル禁止）
TITLE = 'ペット飼育費用シミュレーター | 犬・猫の年間コストを無料試算 | AppADayCreator'
DESCRIPTION = '犬・猫・小動物の種類・体格・医療費を考慮したペット飼育費用を無料シミュレーション。初期費用から年間維持費まで、ペットを迎える前に知っておきたい費用の目安を提示します。'
JS_CODE = 'const questions = [\n    {\n      id: \'pet_type\',\n      text: \'飼いたいペットの種類を教えてください\',\n      options: [\'犬（小型犬・5kg未満）\', \'犬（中型犬・5〜25kg）\', \'犬（大型犬・25kg以上）\', \'猫（室内飼い）\', \'猫（室内外）\', \'小動物（うさぎ・ハムスター等）\', \'鳥類・爬虫類等\']\n    },\n    {\n      id: \'acquisition\',\n      text: \'ペットをどこから迎える予定ですか？\',\n      options: [\'ブリーダー・ペットショップ（純血種）\', \'ペットショップ（混血・一般）\', \'保護猫・保護犬の譲渡\', \'知人・SNSからの譲渡（無料）\']\n    },\n    {\n      id: \'housing\',\n      text: \'住居の状況を教えてください\',\n      options: [\'一戸建て（ペット可）\', \'ペット可のマンション・アパート\', \'現在ペット不可（引越し予定）\']\n    },\n    {\n      id: \'food_grade\',\n      text: \'フード・食事のグレードの希望を教えてください\',\n      options: [\'市販の一般フード（コスパ重視）\', \'国産・無添加フード（中〜高価格帯）\', \'プレミアムフード・手作り食\', \'獣医師推奨の療法食\']\n    },\n    {\n      id: \'grooming\',\n      text: \'トリミング・グルーミングの予定はありますか？\',\n      options: [\'自宅でセルフケアのみ\', \'年2〜4回サロンを利用\', \'月1〜2回定期的にサロン利用\', \'必要ないペット（短毛猫等）\']\n    },\n    {\n      id: \'insurance\',\n      text: \'ペット保険への加入を検討していますか？\',\n      options: [\'ペット保険に加入予定\', \'まだ検討中\', \'保険には加入しない（自費で対応）\', \'既に加入している\']\n    },\n    {\n      id: \'vet_frequency\',\n      text: \'動物病院への通院頻度の想定はどのくらいですか？\',\n      options: [\'年1回の健康診断のみ\', \'年2〜3回（ワクチン・定期検診）\', \'月1回程度（既往症あり・高齢ペット）\', \'できるだけ費用を抑えたい\']\n    }\n  ];\n\n  let currentStep = 0;\n  let answers = {};\n\n  function renderQuestion() {\n    const q = questions[currentStep];\n    document.getElementById(\'step-label\').textContent = `質問 ${currentStep + 1} / ${questions.length}`;\n    document.getElementById(\'progress-bar\').style.width = `${((currentStep + 1) / questions.length) * 100}%`;\n    document.getElementById(\'prev-btn\').classList.toggle(\'hidden\', currentStep === 0);\n    document.getElementById(\'next-btn\').textContent = currentStep === questions.length - 1 ? \'診断する\' : \'次へ\';\n\n    let html = `<p class="font-semibold text-gray-800 mb-4">${q.text}</p><div class="space-y-2">`;\n    q.options.forEach(opt => {\n      const checked = answers[q.id] === opt ? \'checked\' : \'\';\n      html += `<label class="flex items-center gap-3 p-3 border border-gray-200 rounded-xl cursor-pointer hover:border-amber-400 transition has-[:checked]:border-amber-500 has-[:checked]:bg-amber-50">\n        <input type="radio" name="${q.id}" value="${opt}" ${checked} class="accent-amber-500"> <span class="text-sm">${opt}</span>\n      </label>`;\n    });\n    html += \'</div>\';\n    document.getElementById(\'question-area\').innerHTML = html;\n  }\n\n  function saveAnswer() {\n    const el = document.querySelector(`input[name="${questions[currentStep].id}"]:checked`);\n    if (!el) { alert(\'選択してください\'); return false; }\n    answers[questions[currentStep].id] = el.value;\n    return true;\n  }\n\n  function nextStep() {\n    if (!saveAnswer()) return;\n    if (currentStep < questions.length - 1) { currentStep++; renderQuestion(); }\n    else showResult();\n  }\n\n  function prevStep() {\n    if (currentStep > 0) { currentStep--; renderQuestion(); }\n  }\n\n  function calcCosts() {\n    let initialCost = 0;\n    let annualCost = 0;\n\n    // 初期費用（ペット代）\n    const acquisitionMap = {\n      \'ブリーダー・ペットショップ（純血種）\': 20,\n      \'ペットショップ（混血・一般）\': 10,\n      \'保護猫・保護犬の譲渡\': 1,\n      \'知人・SNSからの譲渡（無料）\': 0\n    };\n    initialCost += acquisitionMap[answers.acquisition] || 10;\n\n    // 用品初期費用\n    const typeInitial = {\n      \'犬（小型犬・5kg未満）\': 8,\n      \'犬（中型犬・5〜25kg）\': 10,\n      \'犬（大型犬・25kg以上）\': 15,\n      \'猫（室内飼い）\': 6,\n      \'猫（室内外）\': 5,\n      \'小動物（うさぎ・ハムスター等）\': 3,\n      \'鳥類・爬虫類等\': 5\n    };\n    initialCost += typeInitial[answers.pet_type] || 8;\n\n    // 年間フード代\n    const foodMap = {\n      \'市販の一般フード（コスパ重視）\': 4,\n      \'国産・無添加フード（中〜高価格帯）\': 8,\n      \'プレミアムフード・手作り食\': 15,\n      \'獣医師推奨の療法食\': 20\n    };\n    annualCost += foodMap[answers.food_grade] || 8;\n\n    // ペットによる補正\n    if (answers.pet_type.includes(\'大型犬\')) annualCost *= 1.8;\n    else if (answers.pet_type.includes(\'中型犬\')) annualCost *= 1.3;\n    else if (answers.pet_type.includes(\'小動物\')) annualCost *= 0.4;\n\n    // トリミング\n    const groomMap = {\n      \'自宅でセルフケアのみ\': 0.5,\n      \'年2〜4回サロンを利用\': 3,\n      \'月1〜2回定期的にサロン利用\': 12,\n      \'必要ないペット（短毛猫等）\': 0\n    };\n    annualCost += groomMap[answers.grooming] || 2;\n\n    // 医療費\n    const vetMap = {\n      \''
MAIN_HTML = '<div><button class="btn">開始する</button></div>'
FAQ = [
    ('ペット飼育費用シミュレーター | 犬・猫の年間コストを無料試算 | AppADayCreatorは無料で使えますか？', 'はい、完全無料・登録不要でご利用いただけます。'),
    ('何回でも使えますか？', 'はい、回数制限なく何度でもご利用いただけます。'),
    ('入力したデータはサーバーに送信されますか？', 'いいえ。すべての処理はブラウザ内で完結し、入力内容はサーバーへ送信されません。'),
    ('スマートフォンでも使えますか？', 'はい、スマートフォン・タブレット・PCすべてに最適化されています。'),
    ('結果を保存・共有できますか？', 'スクリーンショットでの保存またはSNSシェアボタンからご共有いただけます。'),
]
HOW_TO = [
    'ページを開き、入力フォームの項目を確認する',
    '必要な情報を入力または選択する',
    '実行ボタンをクリックして結果を取得する',
    '表示された結果・アドバイスを確認する',
    '必要に応じてコピー・SNSシェアで活用する',
]
FOOTER_LINKS = [('https://appadaycreator.com/sleep-quality-checker/', '睡眠の質チェッカー'), ('https://appadaycreator.com/bmi-body-tracker/', 'BMI・体重管理'), ('https://appadaycreator.com/household-budget-analyzer/', '家計簿診断')]
